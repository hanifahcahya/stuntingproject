import React from "react";

const Button = () => {
  return (
    <button className="px-6 py-2 rounded-full font-poppins text-secondary bg-primary hover:scale-110">
      Login
    </button>
  );
};

export default Button;
